{
  "name": "xgpu",
  "private": true,
  "version": "0.8.0",
  "main": "dist/XGPU.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:test": "tsc --project tsconfig_build.json --emitDeclarationOnly",
    "build:types": "tsc --project tsconfig_build.json --emitDeclarationOnly",
    "build": " vite build && npm run build:types",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.26",
    "cross-spawn": "^7.0.3",
    "rimraf": "^5.0.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vite-plugin-checker": "^0.5.6",
    "vite-plugin-external": "^1.2.8"
  },
  "dependencies": {
    "@types/dom-webcodecs": "^0.1.7",
    "@webgpu/types": "^0.1.31",
    "gl-matrix": "^3.4.3"
  }
}